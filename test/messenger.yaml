# messenger: "DummyMessenger"
messenger: "MQTTMessenger"

server: "localhost"
domain: "test"

securedDomain: true   # require a secured domain, accept messages from the dss

# disable message signing for testing, default is true
signMessages: false
